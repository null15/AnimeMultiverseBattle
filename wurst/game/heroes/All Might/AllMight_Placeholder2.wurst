package AllMight_Placeholder2

import abilityDefinitions
import RegisterEvents

function onCast()
	if GetSpellAbilityId() == ALL_MIGHT_PLACEHOLDER2
		skip

init
	registerPlayerUnitEvent(EVENT_PLAYER_UNIT_SPELL_EFFECT, () -> onCast())