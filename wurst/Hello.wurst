package Hello

import ClosureTimers

init
    nullTimer() -> 
        if not loadTOCFile("HideChat.toc")
            printLog(Loglevel.ERROR, "Could not load HideChat.toc")
        // if not loadTOCFile("ShowChat.toc")
        //     printLog(Loglevel.ERROR, "Could not load ShowChat.toc")