package Hello

import UnitIds
import EntitySystem
import ClosureTimers

init
	nullTimer() -> 
		let start = vec2(1000., 1000.)
		for i = 0 to 5
			Unit.create(players[PLAYER_NEUTRAL_AGGRESSIVE], UnitIds.footman, start + vec2(i * 128., 0.), i)
		CreateFogModifierRect(players[0], FOG_OF_WAR_VISIBLE, GetPlayableMapRect(), false, false).start()