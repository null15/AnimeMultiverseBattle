package Hello

import UnitIds

init
	let start = vec2(1000., 1000.)
	for i = 0 to 6
		createUnit(players[PLAYER_NEUTRAL_AGGRESSIVE], UnitIds.footman, start + vec2(i * 128., 0.))
	CreateFogModifierRect(players[0], FOG_OF_WAR_VISIBLE, GetPlayableMapRect(), false, false).start()


	// TO-DO: Pull and Knockback ignores TimeStop, should implement that it doesn't